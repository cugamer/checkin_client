<h1>Your business locations</h1>

<ul>
	<% @locations.each do |loc| %>
		<li class="location-details"><%= loc.location_title %> - <%= loc.lattitude %> <%= loc.hemi_n_s %> by <%= loc.longitude %> <%= loc.hemi_e_w %></li>
	<% end %>
</ul>